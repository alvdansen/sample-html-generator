# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.ruff_cache/

# Virtual environments
.venv/
venv/
env/

# Tool output (default build dir)
grid-output/
# Local `grid watch` output (watcher scratch — never published)
runs/

# OS / editor
.DS_Store
Thumbs.db
design-variants/

# Internal GSD planning — kept local, never published to the public repo
.planning/
